import { ModulesSkeleton } from "@/components/skeletons/ModuleContentSkeleton";

/**
 * Next.js loading.tsx — Shows module layout skeleton while the
 * modules page server component resolves.
 */
export default function ModulesLoading() {
  return <ModulesSkeleton />;
}
